package not.savage.cereal.impl.config.sub;

/**
 * Represents the configuration for the file storage mode. {@link not.savage.cereal.impl.platform.file.CerealFileDatabase}
 * Has no properties. {@link not.savage.cereal.impl.config.Mode.FILE} requires
 * the developer to support the @Location annotation in their classes.
 */
public record FileConfig() {  }
